dist: xenial
language: python
python:
  - "3.7.2"
services: postgresql
env:
  - DJANGO_VERSION=2.2
install:
  - pip install -q Django==$DJANGO_VERSION  
  - pip install -r requirements_build.txt
script:
  - python manage.py test -k
deploy:
  provider: heroku
  api_key: xgyp+7$yjvme!v5l-=a2z&2k&q_-$w@@9f^*8@$971t3u$3+g)
  app: qa